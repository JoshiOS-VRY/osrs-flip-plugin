package com.osrsflipfinder.runelite;

/** Matches {@code DUMP_DETECTION_DESCRIPTION} in the FlipX web app. */
final class DumpDetectionCopy
{
	static final String FILTER_TOOLTIP =
		"Sharp crash vs 1h trading average (confirmed in 5m data). "
			+ "GE guide depth applies only when guide still tracks market prices.";

	private DumpDetectionCopy()
	{
	}
}
